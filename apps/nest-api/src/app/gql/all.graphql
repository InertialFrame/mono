type Query {
  users: [User]
  user(id: ID!): User
}

type Mutation {
  createUser(createUserInput: CreateUserInput!): User!
}

type Subscription {
  userCreated: User!
}

type User {
  id: ID!
  username: String!
}

input CreateUserInput {
  username: String!
}
